{
  "name": "siteforge",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace shared && npm run build --workspace frontend && npm run build --workspace server",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace frontend && npm run typecheck --workspace server",
    "test": "npm run test --workspace frontend && npm run test --workspace server",
    "test:frontend": "npm run test --workspace frontend",
    "test:server": "npm run test --workspace server"
  }
}
